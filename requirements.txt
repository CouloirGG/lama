# LAMA - Dependencies
# Install: pip install -r requirements.txt

# HTTP for price API
requests>=2.31.0

# Windows-specific (cursor tracking, window management)
# These only install on Windows
pywin32>=306; sys_platform == "win32"

# For transparent overlay
# tkinter is built-in with Python on Windows

# Dashboard (desktop app shell)
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
websockets>=12.0
pywebview>=4.4.0

# System tray
pystray>=0.19.5
Pillow>=10.0.0

# Environment variables
python-dotenv>=1.0.0

# Process name (shows "LAMA" in Task Manager instead of pythonw.exe)
setproctitle>=1.3

# Testing
pytest>=7.0
